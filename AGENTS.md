# AGENTS.md

## Cursor Cloud specific instructions

This is a "DataVisualization" repository that was initialized with only a `README.md` — no application code, dependencies, build system, or services exist yet.

- **No dependencies to install** — there are no `package.json`, `requirements.txt`, `pyproject.toml`, or other dependency manifests.
- **No services to run** — there is no application code, so no dev server, database, or external service is required.
- **No tests to run** — there are no test files or test frameworks configured.
- **No lint/build commands** — there are no linter or build configurations.

When application code is added, update this file with relevant setup, run, lint, test, and build instructions.
